import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Flux Electronics - Global Leader in Electronic Components & Parts",
  description: "International leader in premium electronic components, semiconductors, connectors, and development tools. Serving 500,000+ customers across 120+ countries with unmatched quality and expertise.",
  keywords: "electronic components, semiconductors, connectors, development boards, electronics parts, flux electronics, global electronics, international components, electronic distribution",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  );
}
